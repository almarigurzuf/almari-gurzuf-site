export function initBooking() {
    // Вся инициализация теперь вынесена напрямую в HTML файлы 
    // для максимальной надежности загрузки модулей Bnovo
    console.log('Bnovo initialization handled by inline scripts');
}
